<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Barber.Maui.BrandonBarber.Pages.ForgotPasswordPage"
             BackgroundColor="#0E2A36" 
             Shell.NavBarIsVisible="False" xmlns:controls="clr-namespace:Barber.Maui.BrandonBarber.Controls" Grid.RowSpan="1">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#0E2A36" Offset="0.0" />
                <GradientStop Color="#265d82" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>
        <ScrollView VerticalScrollBarVisibility="Never" HorizontalScrollBarVisibility="Never" Padding="30">
            <Grid>
                <VerticalStackLayout Spacing="20" VerticalOptions="Center">
                    <Image Source="logobarbergoblanco.png" WidthRequest="280" HeightRequest="280" HorizontalOptions="Center" />
                    <Label Text="Recuperar Contraseña" FontSize="24" TextColor="White" HorizontalOptions="Center"  FontAttributes="Bold"/>
                    <StackLayout x:Name="EmailStep" Spacing="15">
                        <Label Text="Ingresa tu email para recibir un código de recuperación" FontSize="14"  TextColor="#B0BEC5"  HorizontalOptions="Center"  HorizontalTextAlignment="Center"/>
                        <Border BackgroundColor="#E0F7FA" StrokeShape="RoundRectangle 25"  StrokeThickness="0"  Padding="0"  HorizontalOptions="Fill">
                            <Grid Padding="12,0" ColumnDefinitions="Auto, *" VerticalOptions="Center">
                                <Image Source="usericons.png" WidthRequest="25" HeightRequest="25" VerticalOptions="Center" />
                                <Entry Grid.Column="1" x:Name="EmailEntry" TextColor="#0E2A36"  Placeholder="correo@ejemplo.com" PlaceholderColor="#90A4AE" FontSize="14" HeightRequest="45" BackgroundColor="Transparent"  HorizontalOptions="Fill" VerticalOptions="Center" Keyboard="Email"/>
                            </Grid>
                        </Border>
                        <Grid>
                            <Button Grid.ColumnSpan="2" HorizontalOptions="Center" Text="Enviar Código" Style="{StaticResource RecoveryButtonStyle}"  Clicked="OnSendCodeClicked"/>
                        </Grid>
                    </StackLayout>

                    <StackLayout x:Name="ResetStep" Spacing="15" IsVisible="False">
                        <Label Text="Revisa tu email y ingresa el código recibido junto con tu nueva contraseña"  FontSize="14" TextColor="#B0BEC5" HorizontalOptions="Center" HorizontalTextAlignment="Center"/>

                        <Border BackgroundColor="#E0F7FA" StrokeShape="RoundRectangle 25" StrokeThickness="0" Padding="0" HorizontalOptions="Fill">
                            <Grid Padding="12,0" ColumnDefinitions="Auto, *" VerticalOptions="Center">
                                <Image Source="code.png" WidthRequest="25" HeightRequest="25" VerticalOptions="Center" />
                                <Entry Grid.Column="1" x:Name="TokenEntry" TextColor="#0E2A36" Placeholder="123456" PlaceholderColor="#90A4AE" FontSize="18" HeightRequest="45" BackgroundColor="Transparent" HorizontalOptions="Fill" Keyboard="Numeric" MaxLength="6" FontAttributes="Bold"/>
                            </Grid>
                        </Border>

                        <Border BackgroundColor="#E0F7FA" StrokeShape="RoundRectangle 25" StrokeThickness="0" Padding="0" HorizontalOptions="Fill">
                            <Grid Padding="12,0" ColumnDefinitions="Auto, *" VerticalOptions="Center">
                                <Image Source="lock.png" WidthRequest="25" HeightRequest="25" VerticalOptions="Center" />
                                <Entry Grid.Column="1" x:Name="NewPasswordEntry" TextColor="#0E2A36" Placeholder="Nueva contraseña" PlaceholderColor="#90A4AE" FontSize="14" HeightRequest="45" BackgroundColor="Transparent" HorizontalOptions="Fill" VerticalOptions="Center" IsPassword="True"/>
                            </Grid>
                        </Border>

                        <Border BackgroundColor="#E0F7FA" StrokeShape="RoundRectangle 25" StrokeThickness="0" Padding="0" HorizontalOptions="Fill">
                            <Grid Padding="12,0" ColumnDefinitions="Auto, *" VerticalOptions="Center">
                                <Image Source="lock.png" WidthRequest="25" HeightRequest="25" VerticalOptions="Center" />
                                <Entry Grid.Column="1" x:Name="ConfirmPasswordEntry" TextColor="#0E2A36" Placeholder="Confirmar contraseña" PlaceholderColor="#90A4AE" FontSize="14" HeightRequest="45" BackgroundColor="Transparent" HorizontalOptions="Fill" VerticalOptions="Center" IsPassword="True"/>
                            </Grid>
                        </Border>

                        <Button Text="Restablecer Contraseña" Style="{StaticResource RecoveryButtonStyle}" Clicked="OnResetPasswordClicked" HorizontalOptions="Fill"/>

                        <Button Text="Reenviar Código" Style="{StaticResource SecondaryButtonStyleForgotPasswordPage}" Clicked="OnResendCodeClicked" HorizontalOptions="Fill"/>
                    </StackLayout>
                    <HorizontalStackLayout HorizontalOptions="Center" Margin="0,20,0,0">
                        <Label Text="¿Recordaste tu contraseña?" TextColor="White" FontSize="Body"/>
                        <Label Text=" Iniciar Sesión" TextColor="#FF6F91" FontSize="Body" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnBackToLoginClicked" />
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Grid>
        </ScrollView>
        <controls:LoadingControl x:Name="LoadingIndicator" IsVisible="False"/>
    </Grid>
</ContentPage>