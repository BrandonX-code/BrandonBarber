<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Barber.Maui.BrandonBarber.Pages.GestionarDisponibilidadPage"
             BackgroundColor="#0E2A36" xmlns:controls="clr-namespace:Barber.Maui.BrandonBarber.Controls" Grid.RowSpan="1">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#0E2A36" Offset="0.0" />
                <GradientStop Color="#265d82" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>
        <Grid RowDefinitions="Auto,*,Auto">
            <Label Grid.Row="0" Text="Gestionar Horarios Semanales" FontSize="24" FontAttributes="Bold" TextColor="White"  HorizontalOptions="Center" Margin="20,40,20,20"/>
            <ScrollView Grid.Row="1" VerticalScrollBarVisibility="Never">
                <VerticalStackLayout x:Name="DiasContainer" Padding="20" Spacing="15"/>
            </ScrollView>
            <HorizontalStackLayout Grid.Row="2" Padding="20" Spacing="15" HorizontalOptions="Center">
                <Button Text="Guardar solo esta semana" BackgroundColor="#FF6F91" TextColor="Black" CornerRadius="15" Clicked="OnGuardarSemanaClicked"/>
                <Button Text="Aplicar a todo el mes" BackgroundColor="#FF6F91" TextColor="Black" CornerRadius="15" Clicked="OnGuardarMesClicked"/>
            </HorizontalStackLayout>
        </Grid>
        <controls:LoadingControl x:Name="LoadingIndicator" IsVisible="False"/>
    </Grid>
</ContentPage>