<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Barber.Maui.BrandonBarber.Pages.GestionSolicitudesPage" xmlns:controls="clr-namespace:Barber.Maui.BrandonBarber.Controls" Grid.RowSpan="1">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#0E2A36" Offset="0.0" />
                <GradientStop Color="#265d82" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>
        <!-- CollectionView de Solicitudes -->
        <CollectionView x:Name="SolicitudesCollection" Margin="20">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" Margin="0,0,0,10">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#CFCFCF" Offset="0.0" />
                                <GradientStop Color="White" Offset="1.0" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto" RowSpacing="10" Padding="10">

                            <!-- Nombre -->
                            <Grid ColumnDefinitions="Auto,*" Grid.Row="0" ColumnSpacing="5">
                                <Image Grid.Column="0" Source="id.png" WidthRequest="20" HeightRequest="20" />
                                <Label Grid.Column="1" Text="{Binding NombreSolicitante}" FontAttributes="Bold" FontSize="18" LineBreakMode="WordWrap"/>
                            </Grid>

                            <!-- Email -->
                            <Grid ColumnDefinitions="Auto,*" Grid.Row="1" ColumnSpacing="5">
                                <Image Grid.Column="0" Source="email.png" WidthRequest="20" HeightRequest="20" />
                                <Label Grid.Column="1" Text="{Binding EmailSolicitante}" FontAttributes="Bold" LineBreakMode="WordWrap"/>
                            </Grid>

                            <!-- Teléfono (movido debajo del email) -->
                            <Grid ColumnDefinitions="Auto,*" Grid.Row="2" ColumnSpacing="5">
                                <Image Grid.Column="0" Source="call.png" WidthRequest="20" HeightRequest="20" />
                                <Label Grid.Column="1" Text="{Binding TelefonoSolicitante}" FontAttributes="Bold"/>
                            </Grid>

                            <!-- Justificación -->
                            <VerticalStackLayout Grid.Row="3" Spacing="5">
                                <Grid ColumnDefinitions="Auto,*" Grid.Row="3" ColumnSpacing="5">
                                    <Image Grid.Column="0" Source="description.png" WidthRequest="20" HeightRequest="20" />
                                    <Label Grid.Column="1" Text="Justificación:" FontAttributes="Bold" />
                                </Grid>
                                <Label Text="{Binding Justificacion}" LineBreakMode="WordWrap" VerticalOptions="Start" HorizontalOptions="Fill" FontSize="14" />
                            </VerticalStackLayout>

                            <!-- Fecha -->
                            <Grid ColumnDefinitions="Auto,*" Grid.Row="4" ColumnSpacing="5">
                                <Image Grid.Column="0" Source="calendario.png" WidthRequest="20" HeightRequest="20" />
                                <Label Grid.Column="1" Text="{Binding FechaSolicitud, StringFormat='{0:dd/MM/yyyy}'}" FontAttributes="Bold"/>
                            </Grid>

                            <!-- Botones -->
                            <Grid  ColumnDefinitions="Auto,*" Grid.Row="5" ColumnSpacing="10" Margin="0,10,0,0" HorizontalOptions="Center">
                                <Button Grid.Column="0" Text="Aprobar" BackgroundColor="#FF6F91" CornerRadius="15" Clicked="OnAprobarClicked" CommandParameter="{Binding .}" TextColor="Black"/>
                                <Button Grid.Column="1" Text="Rechazar" BackgroundColor="#717d7e" CornerRadius="15" Clicked="OnRechazarClicked" CommandParameter="{Binding .}" TextColor="Black"/>
                            </Grid>

                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Mensaje cuando no hay solicitudes -->
        <VerticalStackLayout x:Name="EmptyStateLayout" IsVisible="False" VerticalOptions="Center"  HorizontalOptions="Center" Spacing="15">
            <Image Source="chat.png" WidthRequest="80" HeightRequest="80" Opacity="0.6"/>
            <Label Text="No hay solicitudes pendientes" FontSize="18" FontAttributes="Bold" TextColor="White" HorizontalTextAlignment="Center"/>
            <Label Text="Las nuevas solicitudes aparecerán aquí" FontSize="14" TextColor="#90A4AE"
                   HorizontalTextAlignment="Center"/>
        </VerticalStackLayout>
        <controls:LoadingControl x:Name="LoadingIndicator" IsVisible="False"/>
    </Grid>
</ContentPage>
