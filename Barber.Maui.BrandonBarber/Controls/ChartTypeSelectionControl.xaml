<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Barber.Maui.BrandonBarber.Controls.ChartTypeSelectionControl">
    <Grid ColumnDefinitions="*,*" ColumnSpacing="5" Padding="-5">

        <Border x:Name="BarrasButton" Grid.Column="0" BackgroundColor="#0E2A36" StrokeShape="RoundRectangle 16" Padding="12" StrokeThickness="1" Stroke="#0E2A36">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" VerticalOptions="Center">
                <Image x:Name="BarrasIconWhite" Source="barrasblancas.png" WidthRequest="24" HeightRequest="24" VerticalOptions="Center" Grid.Column="0"/>
                <Image x:Name="BarrasIconBlack" Source="barrasnegras.png" WidthRequest="24" HeightRequest="24" VerticalOptions="Center" Grid.Column="0" IsVisible="False"/>
                <Label Text="Barras" TextColor="White" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" Grid.Column="1"/>
            </Grid>
        </Border>

        <Border x:Name="LineasButton" Grid.Column="1" BackgroundColor="#90A4AE" StrokeShape="RoundRectangle 16" Padding="12" StrokeThickness="1" Stroke="#707070">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" VerticalOptions="Center">
                <Image x:Name="LineasIconWhite" Source="lineasblancas.png" WidthRequest="24" HeightRequest="24" VerticalOptions="Center" Grid.Column="0" IsVisible="False"/>
                <Image x:Name="LineasIconBlack" Source="lineasnegras.png" WidthRequest="24" HeightRequest="24" VerticalOptions="Center" Grid.Column="0"/>
                <Label Text="Líneas" TextColor="Black" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" Grid.Column="1"/>
            </Grid>
        </Border>

    </Grid>
</ContentView>
